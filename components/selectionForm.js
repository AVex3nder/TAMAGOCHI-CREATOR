import React, {useState, useEffect} from 'react';
import {View, Text, Image} from 'react-native';

const SelectionForm = (userTamagochi, userBackground) => {
  return <Text>form space</Text>;
};

export default SelectionForm;
